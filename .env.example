# Copie ce fichier en .env.local (jamais commité) et remplis les deux valeurs
# depuis ton projet Supabase : Project Settings → Data API.
# Sans ces deux valeurs, l'app fonctionne normalement mais sans les sessions de groupe.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
